<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carlosmiyashiro.busquedadanielv1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BuscarActivity" />
        <activity android:name=".DetalleBusquedaActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ControlBultos" />
        <activity android:name=".ListarPedidos" />
        <activity android:name=".MenuPrincipal" />
        <activity android:name=".ejemploConsulta" />
        <activity android:name=".ListaDocumentosActivity" />
        <activity android:name=".SpinnerActivity"></activity>
    </application>

</manifest>